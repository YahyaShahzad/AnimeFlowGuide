version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=${DATABASE_URL:-sqlite:///anime.db}
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - .:/app:rw
    restart: unless-stopped
